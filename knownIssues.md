The following negative tests currently fail because the API does not perform input validation:

Create order – negative quantity returns 400

Create order – zero quantity returns 400

Create order – invalid product ID returns 400